rootProject.name = "playground-ksp-dev-experimental"

include("annotation")
include("app")
include("processor")

pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
    }
}
